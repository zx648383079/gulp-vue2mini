import { TemplateProject } from './project';

export class ScriptParser {
    constructor(
        _: TemplateProject
    ) {}

    public render(content: string): string {
       return content;
    }
}